<FileBrowser>:
    cols: 1
	rows: 1
	padding: 30
	FileChooserIconView:
	    id: filechooser
    	text: 'Load server cert'
    	text_size: self.size
    	path: root.get_path()
    	on_selection: root.select(filechooser.selection)